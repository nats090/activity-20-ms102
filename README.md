# Secure Full-Stack Starter

A boilerplate React + Express app with OWASP Top-10 mitigations, secure headers, rate limiting, JWT-based auth, input validation, and safe HTML rendering.

## Setup

1. Clone this repo  
2. Install backend deps  
   ```bash
   cd backend
   npm install

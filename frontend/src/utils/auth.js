// Placeholder for client-side auth helpers.
// We store tokens in httpOnly cookies, so actual checks happen server-side.

export const isAuthenticated = () => {
  // Optionally call an endpoint like '/api/auth/me'
  return false;
};
